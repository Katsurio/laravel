@extends('layouts.app')





@section('content')

    <div class="container">
        <div class="content">
            <div class="title">Post Page Coming Soon...</div>
            <div class="title">{{ $id }}</div>
            <div class="title">{{ $name }}</div>
            <div class="title">{{ $course }}</div>
            <div class="title">{{ $grade }}</div>
        </div>
    </div>

@endsection
